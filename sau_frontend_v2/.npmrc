# Allow esbuild's postinstall to run so Vite can resolve the platform binary.
# pnpm 10+ blocks build scripts by default; without this, `pnpm dev` and
# `pnpm build` will fail with "Cannot find module esbuild".
enable-pre-post-scripts=true
ignore-scripts=false
